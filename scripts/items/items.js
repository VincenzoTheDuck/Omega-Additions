const heavyAlloy = extend(Item, "heavy-alloy", {});

const terraliteAlloy = extend(Item, "terralite-alloy", {});

const sulfur = extend(Item, "sulfur", {});

const izomite = extend(Item, "izomite", {});

const incandestone = extend(Item, "incandestone", {});

const diamond = extend(Item, "diamond", {});

const magnetite = extend(Item, "magnetite", {});

module.exports = {
  heavyAlloy: heavyAlloy,
  terraliteAlloy: terraliteAlloy,
  sulfur: sulfur,
  izomite: izomite,
  incandestone: incandestone,
  diamond: diamond,
  magnetite: magnetite,
}
