const acid = extend(Liquid, "acid", {});

const incandescite = extend(Liquid, "incandescite", {});

const cascadia = extend(Liquid, "cascadia", {});

const supercoolant = extend(Liquid, "supercoolant", {});

const polygen = extend(Liquid, "polygen", {});
